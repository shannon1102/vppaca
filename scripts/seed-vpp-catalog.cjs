/**
 * VPP catalog seeds automatically on first Supabase connect (sbEnsureSeed).
 * Local dev: rm -rf .data && npm run dev
 */
console.log("VPPACA: catalog seed runs via app startup (Supabase) or .data/ (local).");
